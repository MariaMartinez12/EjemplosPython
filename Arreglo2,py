listaEst = []
resp = "Si"
while(resp.upper() != "NO"):
    tam =len(listaEst)
    nombres = input(" Escriba el nombre del estudiante ")
    listaEst.append(nombres)
    resp = input("Deseas agragar mas ? SI - NO ")

for est in listaEst:
    print(est)
